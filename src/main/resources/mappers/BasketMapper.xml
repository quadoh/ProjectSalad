<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="com.teamsalad.mapper.BasketMapper">
 
 <!-- 장바구니 정보 -->
 <insert id="cartInfo">
 insert into tbl_cart(cart_num, m_id, rcp_num, cart_amount, cart_option, cart_price, cart_regdate)
 values(#{cart_num}, #{m_id}, #{rcp_num}, #{cart_amount}, #{cart_option}, #{cart_price}, #{cart_regdate})
 </insert>
  
 </mapper>